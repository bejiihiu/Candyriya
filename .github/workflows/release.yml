name: Release

on:
  push:
    tags:
      - 'v[0-9]+'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
    
    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    
    - name: Build with Gradle
      run: ./gradlew cleanBuild build collect --no-daemon
    
    - name: Get build ID
      id: get_build_id
      run: |
        BUILD_ID=$(git rev-list --count HEAD)
        echo "BUILD_ID=$BUILD_ID" >> $GITHUB_OUTPUT
        echo "TAG_NAME=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.get_build_id.outputs.BUILD_ID }}
        name: Candyriya Build #${{ steps.get_build_id.outputs.BUILD_ID }}
        draft: false
        prerelease: false
        files: |
          build/libs/*.jar
        body: |
          ## Candyriya Build #${{ steps.get_build_id.outputs.BUILD_ID }}
          
          Build ID: ${{ steps.get_build_id.outputs.BUILD_ID }}
          Git Hash: ${{ github.sha }}
          
          ### Supported Platforms
          - Forge
          - NeoForge
          - Fabric
          
          ### Minecraft Version
          - 1.21.1
          
          ---
          Original project by [IzzelAliz](https://github.com/IzzelAliz/Arclight)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
